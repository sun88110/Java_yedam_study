12/22 ~ 1/8 자바
1/8일날 평가인데 앞당겨질 수 있음
그럼 단위수업은 1개 제외하고 다끝나는데
2차 프로젝트하고 난 뒤에 배포
수업으로 단위수업 끝

===============================[1교시]
우리나라는 전자정부 표준프레임워크 기준으로 돌아간다!
이 녀석의 기본 베이스는 스프링을 기반으로 되어있다!
그!런!데 이것만으로 구성되어있는건 아니다!
사실상 뭘 개발하던 자바스프링은 다룰 수 있어야 한다!
전자정부 프레임워크 임마들은 앞서나가는 스타일이 아니다!
천천히 개편함 그래서 바짝바짝 따라가라!

교재만 제대로 봐도 기술면접은 문제가 읎다!
기계어(001010101001)가
어셈블리(중간어)보다
물리적으로 빠르다
자연어(고급언어)는 머신스피릿이 이해할 수 없다!

이 자연어를 이해할 수 있게 머신스피릿에게 접신하는것이
컴파일러다! 이놈이 일반 프로그래밍언어에서
매우 중★요!

프로그래밍언어에는 자바스크립트는 원래 DOM(화면)을
다루는 언어였다 지금은 node가 그 자리를 먹었따!

임베디드(설치형 프로그램)은 아직은 자바 보다 C가 더 강세
로봇과 장치(디바이스)들 또한 C/C++이 강세
C++/C#은 전산기계, 웹 SW 분야 강세
Python은 빅데이터/머신러닝에서 강세 (무조껀 쓸줄알아야됨)

위와 같이 언어들은 각각 특기분야가 있다!

자바는 객체 지향 프로그래밍(OOP : Obeject_ Oriented Programming)
높은 이식성(모든 운영체제 사용가능)
메모리 자동 정리(갈비지 컬렉션)

자바는 JVM이라는 Java Vitual Machine이 탑재되어있다!
보통은 OS가 있고 그 위에 process가 있는데
이 둘 중 어디서 실행 하느냐에 따라 다른데
자바는 procss 안에 JVM를 설치하고 class(byte code)
라는 놈이 process안에서 class가 JVM이 에서 상호작용한다!
한줄로 줄이자면 JVM이 있는곳에는 모두 자바를 쓸 수 있다!

---자바환경구축
JDK (java development kit)
자바 전용 개발도구
JDK 안에는 JRE(Java Runtime Environment)
라는 놈이 포함되어 있따 저놈 안에 JVM이 있다
기본적으로 이 놈만 설치하면 문제가 없다



=========================[2교시]

오라클이 자바라이선스를 갖고있기 때문에
오라클로 들어가서 리소스 탭을 선택하면 
개발자용과 자바가 있는데
개발자로 들어가면 더많은 리소스를 볼 수 있다
거기서 자바를 선택한후 개발자용 JDK를 선택
거기서 윈도우 선택해서 21.0.9버전
x64 인스톨 설치(설정할 것 없음 한무 next)

그리고 교재 33page -> 37page

자바는 이클립스를 사용한다
그런데 이클립스는 업뎃 주기가 빠른 대신 호환성 버그가 많다
그래서 한버전 낮은거나 두버전 낮은걸 써라

Eclipse IDE for Enterprise Java and Web Developers 다운

압축풀기 후 바로 실행

IDE는 종합 개발툴이다 이를 편하게 쓰려면 Vscode를 설치해라

근데 그냥 쓰자

압축푼거 
dev 파일에 넣고 경로 정해서 들어가면 
익스클루드 
익스클루션 체크 어쩌고 나오는데
그냥 밑에꺼써라

프로젝트는 기본적으로 소문자로 시작한다 카멜 표기법 사용

이클립스 설명
오른쪽 상단에 UI선택이 있다 
퍼스펙티브 아이콘 = UI 

자바 프로젝트 생성해보기 

=========================[3교시]

코드 작성하면 src 폴더 안에 들어가야됨

src 우클릭 후 new를 눌러서 각 요소를 선택하여
클래스 생성하면서 패키지를 생성함 기본적으로 패키지 이름은 3단어 이상

이때 클래스를 생성할땐 소스폴더는 건들지않는것이 좋다!
클래스 이름은 대문자로 시작! 파스칼 표기법 사용

package com.yedam.test; <- 패키지가 항상 가장먼저 나와야되고 

public class Hello { <- 여기 까지 있는게 클래스의 원형임 

	public static void main(String[] args) { 
		// Java를 실행 할 때 핵심 메서드 = 메인 메서드 안에서 코드작성

	}

}


=========================[4교시]
실제로 JVM이 실행하는 파일은 class

bin, classpath, .setting는 깃허브에 올라가면 안된다

변수는 메모리 주소 이름

강타입 언어 = 정확한 변수타입 선언

변수 대소문자 구분을 한다.
첫문자는 글이나 문자 $ 혹은 _ 여야 하고 숫자로 시작할 수 있다
소문자로 시작하되 다른 단어가 붙으면 첫문자를 대문자 (카멜 표기법)
헷갈리면 소문자로써라
자바 예약어는 사용할 수 없다 

값을 저장할 경우 대입(=) 연산자 사용

자바는 null을 값으로 치지않는다.

변수선언할때 초기값을 주는걸 권장 
그렇지않으면 변수는 메모리에서 값을
읽을 수 없음

bit, byte, short, char, int, long
0,1
-128 ~ 127
-32,768 ~ 32,767
0 ~ 65535
-2,147,483,648 ~ 2,147,483,547
-대박큰수 ~ 짱큰수

리터럴 = 직접 입력된 값

=========================[6교시]

Long을 쓸때 10000000000L <- 뒤에 L을 적어줘야 리터럴이 적용됨

int v2 = 'C'; 이렇게쓰면 정수로 표시됨
char은 단어1개만 담을수있는데 str은 문자열을 담을 수 있음
/t 탭
/n 줄바꿈
/출력
// 출력
//j말

float 타입은 3.14f , 3E6F;

둘중 하나만 써라 float이나 double 왜냐하면
사실 0.1이라고 되어있지만 그 뒤에 
float은 7자리 , double은 15자리 까지 표기해주니까
몇만번 더하다보면 차이가 있다.

============================[7교시]

더 큰 타입의 정수가 있을경우 타입이 가장 큰 녀석으로 전환됨

// 1) int 보다 밑의 타입을 기준으로 연산 => int 자동변환
		byte x = 30;
		byte y = 50;
		//byte z = (byte)(x + y);
		int z = x + y;
		
// 2) long 타입이 포함될 경우 모든 long으로 자동변환
		int a = 100;
		long b = 150L;
		long c = a + b;

// 1) 동일한 타입일 경우 해당 타입으로 연산
		float a = 0.12F;
		float b = 0.1F;
		float c = a + b;

// 2) double 타입이 포함된 경우 모두 double로 변환
		float v1 = 0.12F;
		double v2 = 0.1;
		//float v3 = (float)(v1 + v2);
		//double v3 = v1 + v2;

============================[8교시]

String str1 = String.valueOf(1 + 2 + 3); //String.valueOf()
		str1 = 1 + 2  + 3 + ""; 		// "6"
		String str2 = 1 + 2 + "3"; 		// "33
		String str3 = 1 + "2" + 3; 		// "123"
		String str4 = "1" + 2  + 3; 	// "123"
		String str5 = "1" + ( 2 + 3 );	// "15'
		System.out.println(str1);
		System.out.println(str2);
		System.out.println(str3);
		System.out.println(str4);
		System.out.println(str5);	
		
		// 강제형변환 : 각 타입별 메서드 사용
		int val1 = Integer.parseInt("1234");
		double val2 = Double.parseDouble("2.5");
		double result = val1 + val2;
		System.out.println(result);
	

printf("형식실수값".값) 소수점 몇번째 자리까지 표시;
printd("형식정수값".값) 왼쪽 몇번째자리 정수
prints("형식문자열".값) 왼쪽 몇번째 문자열

System.out.printf("이름 : %s,\n나이 : %d,\n월급: %.4f", "홍길동", 32, 250.25);
이름 : 홍길동,
나이 : 32,
월급: 250.2500

============================[9교시]

컨트롤 + 쉬프트 + O (임포트 자동 불러오기)

scanner 입력받기

